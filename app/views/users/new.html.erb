<h1>New user</h1>

<% form_for(@user) do |f| %>
    <%= f.error_messages %>
    <%= render "user" %>

    <div class="buttonPanel">
        <%= f.submit 'Create' %>
        <%= link_to 'Back', users_path %>
    </div>
<% end %>

